class AppConstants {
  AppConstants._();

  static const Duration locationTick = Duration(seconds: 3);
  static const Duration apiLatency = Duration(milliseconds: 600);

  static const double defaultMapZoom = 15.5;
  static const double followMapZoom = 17.0;

  static const double panelRadius = 28.0;
  static const double cardRadius = 20.0;
  static const double buttonRadius = 16.0;
}
